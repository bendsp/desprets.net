Emoji Color Picker is a small experiment that matches a color to the emoji with
the closest average RGB value.

It started as a playful idea, but it turned into a nice little exercise in
working with image data, color comparison, and constrained interfaces.

The result is intentionally lightweight and a bit strange, which is part of why
I like it.
