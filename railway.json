{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run export"
  },
  "deploy": {
    "startCommand": "npx serve@latest out -l tcp://0.0.0.0:$PORT"
  }
}
